# 프로필 이미지 디렉토리

여기에 프로필 이미지 파일을 넣으세요.

- 기본 파일명: `profile.jpg`
- 다른 파일명/확장자를 쓰려면 `data/profile.json` 의 `imagePath` 값을 수정하세요.
  - 예: `"imagePath": "/images/me.png"`
- 경로는 `public` 폴더 기준이므로 항상 `/images/...` 로 시작합니다.

이미지를 넣지 않으면 화면에는 이름 첫 글자가 들어간 기본 아바타가 표시됩니다.
